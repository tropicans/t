---
phase: 05-routing-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/lib/validators.ts
  - src/app/actions/short.ts
  - src/app/actions/microsite.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
must_haves:
  truths:
    - "Native Next.js middleware protects dashboard paths"
    - "Slug/alias collision checks prevent route shadowing"
    - "URL validation permits only HTTP/HTTPS protocols and automatically prepends HTTPS if needed"
  artifacts:
    - src/middleware.ts
    - src/lib/validators.ts
  key_links:
    - "src/middleware.ts routes auth protection to NextAuth"
    - "src/lib/validators.ts contains reserved slugs and collision validation"
---

## Phase Goal

**As a** system security engineer, **I want to** establish Next.js middleware protection, prevent alias/slug collision, and validate URL schemes, **so that** public routing is secure and protected routes are guarded.

<objective>
Implement routing security enhancements per decisions D-01 through D-10.

Purpose: move protected route checks to a native Next.js middleware, prevent path name shadowing, and sanitize input redirect URLs.
Output: middleware configuration, validators utility, and refactored actions.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-routing-security-hardening/05-CONTEXT.md
</context>

## Artifacts this phase produces

- New file: `src/lib/validators.ts`
- Move file: `src/proxy.ts` to `src/middleware.ts`

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| REQ | SEC-01 | COVERED | Task 1 |
| REQ | SEC-02 | COVERED | Task 2 |
| REQ | SEC-03 | COVERED | Task 3 |

<tasks>

<task type="auto">
  <name>Task 1: Relocate Auth Route Protection to Native Middleware</name>
  <files>src/middleware.ts</files>
  <read_first>
    @src/proxy.ts
  </read_first>
  <behavior>
    - Move auth protection from `src/proxy.ts` to `src/middleware.ts`.
  </behavior>
  <action>Use git to move `src/proxy.ts` to `src/middleware.ts` and ensure it targets `/dashboard/:path*` with redirects to `/login` for unauthorized requests.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/middleware.ts` exists and functions correctly.
  </acceptance_criteria>
  <done>Next.js middleware protection enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize Route Collision Detection and Reserved Routes</name>
  <files>src/lib/validators.ts, src/app/actions/short.ts, src/app/actions/microsite.ts</files>
  <read_first>
    @src/app/actions/short.ts
    @src/app/actions/microsite.ts
  </read_first>
  <behavior>
    - Check slugs and aliases against each other and a reserved route list.
  </behavior>
  <action>Create `src/lib/validators.ts` with checks against reserved routes (e.g. login, dashboard, api, _next) and query both short links and microsites to avoid collision. Update server actions to call this validator.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Validators file checks for collisions and reserved routes.
  </acceptance_criteria>
  <done>Route collision prevention active.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce URL Protocol Sanitization and Auto-Correction</name>
  <files>src/lib/validators.ts, src/app/actions/short.ts, src/app/actions/microsite.ts</files>
  <read_first>
    @src/lib/validators.ts
  </read_first>
  <behavior>
    - Permit only http/https schemes. Prepend https:// if protocol is missing but domain is valid. Reject local loopbacks in production.
  </behavior>
  <action>Write URL validation and correction function in `src/lib/validators.ts` and apply to link destination parameters in server actions.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - URL schemes validated and automatically corrected if missing.
  </acceptance_criteria>
  <done>URL scheme security checks implemented.</done>
</task>

</tasks>

<success_criteria>
Route protection is migrated to native Next.js middleware, route shadowing is prevented, and destination URLs are securely validated and corrected.
</success_criteria>

<output>
Create `.planning/phases/05-routing-security-hardening/05-01-SUMMARY.md` when done.
</output>
