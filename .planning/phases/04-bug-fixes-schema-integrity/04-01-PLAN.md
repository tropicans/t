---
phase: 04-bug-fixes-schema-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/short-link-redirect.ts
  - src/app/api/click/microsite-link/[linkId]/route.ts
  - scripts/verify-phase4.mjs
autonomous: true
requirements:
  - DB-01
  - BUG-01
  - BUG-02
must_haves:
  truths:
    - "verifyPasswordAndRedirect checks shortLink.expiresAt before password bcrypt compare and redirects to /shortCode on expired"
    - "api/click/microsite-link/[linkId] redirects to public profile/[username] when link is inactive but parent microsite is published"
    - "api/click/microsite-link/[linkId] returns 404 JSON response when parent microsite is unpublished, disabled, or missing"
    - "Prisma client successfully loads schema with avatarImage in Microsite table"
  artifacts:
    - scripts/verify-phase4.mjs
  key_links:
    - "src/app/actions/short-link-redirect.ts checks link.expiresAt before comparing passwords"
    - "src/app/api/click/microsite-link/[linkId]/route.ts checks link.microsite.isPublished and link.isActive"
---

## Phase Goal

**As a** system developer, **I want to** resolve the database schema drift and fix redirect logic gaps, **so that** link expirations are enforced securely and inactive or unpublished microsite links are handled gracefully.

<objective>
Implement security and integrity fixes for database schema drift and redirect logic paths per decisions D-01 through D-06.

Purpose: enforce link expiration for password-protected routes, ensure inactive or unpublished microsite links are rejected, and verify schema integrity.
Output: modified server actions, updated api route handler, and a validation verification script.
</objective>

<execution_context>
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-bug-fixes-schema-integrity/04-CONTEXT.md
@src/app/actions/short-link-redirect.ts
@src/app/api/click/microsite-link/[linkId]/route.ts
</context>

## Artifacts this phase produces

- New file: `scripts/verify-phase4.mjs`

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| REQ | DB-01 | COVERED | Task 1 and Task 3 |
| REQ | BUG-01 | COVERED | Task 2 and Task 3 |
| REQ | BUG-02 | COVERED | Task 2 and Task 3 |
| CONTEXT | D-01, D-02 | COVERED | Task 2 |
| CONTEXT | D-03, D-04, D-05 | COVERED | Task 2 |
| CONTEXT | D-06 | COVERED | Task 1 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate validation test script and check database schema status</name>
  <files>scripts/verify-phase4.mjs</files>
  <read_first>
    @.planning/phases/04-bug-fixes-schema-integrity/04-CONTEXT.md
    @src/app/actions/short-link-redirect.ts
    @src/app/api/click/microsite-link/[linkId]/route.ts
  </read_first>
  <behavior>
    - RED: Script exits non-zero before Task 2 because files do not yet contain expiration checks or microsite link visibility checks.
    - GREEN: Script exits zero after Task 2 when files contain the required checks and database queries for Microsite.avatarImage succeed.
  </behavior>
  <action>Create `scripts/verify-phase4.mjs` using Node `fs`/`path` and PrismaClient. Assert that `src/app/actions/short-link-redirect.ts` contains `link.expiresAt` check inside `verifyPasswordAndRedirect` before `bcrypt.compare`. Assert that `src/app/api/click/microsite-link/[linkId]/route.ts` contains checks for `link.microsite.isPublished` and `link.isActive`. Assert that a Prisma query requesting `avatarImage` from the `Microsite` table executes without column-not-found errors.</action>
  <verify>
    <automated>node scripts/verify-phase4.mjs exits non-zero before implementation, and exits 0 after implementation</automated>
  </verify>
  <acceptance_criteria>
    - Source check: `scripts/verify-phase4.mjs` exists and contains correct assertions.
    - DB check: The script successfully queries the database using Prisma without any schema drift errors.
  </acceptance_criteria>
  <done>Static and database validation test script exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement password redirect expiration check and microsite link visibility guards</name>
  <files>src/app/actions/short-link-redirect.ts, src/app/api/click/microsite-link/[linkId]/route.ts</files>
  <read_first>
    @src/app/actions/short-link-redirect.ts
    @src/app/api/click/microsite-link/[linkId]/route.ts
  </read_first>
  <behavior>
    - `verifyPasswordAndRedirect` checks if `link.expiresAt` is in the past, and redirects to `/${shortCode}` before `bcrypt.compare` per D-01 and D-02.
    - `/api/click/microsite-link/[linkId]` returns 404 JSON if the parent microsite is not published per D-03 and D-05.
    - `/api/click/microsite-link/[linkId]` redirects back to the microsite URL `/${link.microsite.slug}` if the link is inactive but the parent microsite is published per D-04.
  </behavior>
  <action>In `src/app/actions/short-link-redirect.ts`, modify `verifyPasswordAndRedirect` to verify `link.expiresAt && link.expiresAt < new Date()` immediately after fetching the link and before the `bcrypt.compare` operation. If expired, call `redirect("/" + shortCode)`. In `src/app/api/click/microsite-link/[linkId]/route.ts`, fetch the link and check `link.microsite.isPublished`. If false, return a 404 JSON response. If the microsite is published but `link.isActive` is false, redirect to the parent microsite slug page using `NextResponse.redirect(new URL("/" + link.microsite.slug, req.url))`. Ensure all imports are correct and TypeScript compiles.</action>
  <verify>
    <automated>node scripts/verify-phase4.mjs</automated>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/app/actions/short-link-redirect.ts` checks expiration before bcrypt compare.
    - Source assertion: `src/app/api/click/microsite-link/[linkId]/route.ts` validates `isPublished` and `isActive` with correct fallback redirects and JSON responses.
    - compilation: `npx tsc --noEmit` and `npm run lint` pass successfully.
  </acceptance_criteria>
  <done>Expiration checks and visibility guards implemented and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> server action | Submitted passwords for expired links should not trigger expensive bcrypt computation or direct redirection. |
| public client -> click API route | Click tracking must not allow redirect to disabled/unpublished links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Denial of Service | `verifyPasswordAndRedirect` | medium | mitigate | Check link expiration before running `bcrypt.compare` to save CPU resources. |
| T-04-02 | Information Disclosure / Bypasses | `verifyPasswordAndRedirect` | high | mitigate | Hard redirect to standard expired view to avoid disclosing original URL on expired link. |
| T-04-03 | Elevation of Privilege / Tampering | `/api/click/microsite-link/[linkId]` | high | mitigate | Enforce `isPublished` and `isActive` filters at the API layer. |
</threat_model>

<verification>
Run `node scripts/verify-phase4.mjs`, `npm run lint`, and `npx tsc --noEmit`. No additional Prisma generate is needed since database types are already updated.
</verification>

<success_criteria>
Prisma schema drift is resolved and validated; password-protected link expiration redirects before password checks; inactive and unpublished microsite links are rejected or redirected correctly; all static checks and type checks pass.
</success_criteria>

<output>
Create `.planning/phases/04-bug-fixes-schema-integrity/04-01-SUMMARY.md` when done.
</output>
