---
phase: 01-microsite-theme-variants
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/dashboard/microsites/page.tsx
  - scripts/verify-microsite-themes.mjs
autonomous: false
requirements:
  - THEME-02
  - THEME-04
  - THEME-05
must_haves:
  truths:
    - "Dashboard microsite list thumbnails use same shared registry metadata as theme picker and public renderer per D-04, THEME-05."
    - "Thumbnails for new themes show professional, brandable color cues per D-14 and D-15."
    - "Existing dark, light, gradient thumbnails stay recognizable while centralizing lookup per D-02."
    - "Final human UAT checks theme readability on public mobile and desktop per D-17."
  artifacts:
    - src/app/dashboard/microsites/page.tsx registry-powered thumbnail consumer
    - scripts/verify-microsite-themes.mjs thumbnail assertions
  key_links:
    - "`ThemeThumbnail` calls `getMicrositeTheme(theme).thumbnail` instead of if-chain drift."
    - "Verification script covers dashboard list registry import and absence of hardcoded theme branches."
---

## Phase Goal

**As a** microsite owner, **I want to** choose from expanded preset themes, **so that** my public microsite better matches my brand.

<objective>
Finish registry centralization by wiring dashboard list thumbnails and performing final visual/UAT checkpoint for D-02, D-04, D-14, D-15, D-17.

Purpose: prevent thumbnail drift after expanded theme launch and prove themes are recognizable/readable across dashboard and public pages.
Output: list page thumbnail registry consumer, updated verification assertions, human visual checkpoint.
</objective>

<execution_context>
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
@.planning/phases/01-microsite-theme-variants/01-RESEARCH.md
@.planning/phases/01-microsite-theme-variants/01-PATTERNS.md
@src/lib/microsite-themes.ts
@src/app/dashboard/microsites/page.tsx
</context>

## Artifacts this phase produces

- Modified function/component: `ThemeThumbnail({ theme, title })`
- New registry consumer: `getMicrositeTheme` import in `src/app/dashboard/microsites/page.tsx`
- New verifier assertions for dashboard thumbnail registry usage in `scripts/verify-microsite-themes.mjs`
- No new files beyond Plan 01 artifacts

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Expanded theme choices reflected beyond editor/public path | COVERED | Task 1 thumbnail parity |
| REQ | THEME-02 recognizable visual styling | COVERED | Task 1 and checkpoint |
| REQ | THEME-04 consistent styling surfaces | COVERED | Task 1 registry thumbnail parity plus public UAT |
| REQ | THEME-05 centralized theme list | COVERED | Task 1 verifier assertions |
| RESEARCH | Replace `ThemeThumbnail` if-chain with registry metadata | COVERED | Task 1 |
| CONTEXT | D-02, D-04, D-14, D-15, D-17 | COVERED | Task 1 and checkpoint |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire dashboard list thumbnails to shared registry</name>
  <files>src/app/dashboard/microsites/page.tsx, scripts/verify-microsite-themes.mjs</files>
  <read_first>
    @src/app/dashboard/microsites/page.tsx
    @src/lib/microsite-themes.ts
    @scripts/verify-microsite-themes.mjs
    @.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
    @.planning/phases/01-microsite-theme-variants/01-PATTERNS.md
  </read_first>
  <behavior>
    - `ThemeThumbnail` uses registry thumbnail classes for all seven themes per D-04 and THEME-05.
    - Unknown/legacy theme still shows default dark thumbnail via `getMicrositeTheme` per D-10.
    - Existing server component auth/query flow remains unchanged.
  </behavior>
  <action>In `src/app/dashboard/microsites/page.tsx`, import `getMicrositeTheme`. Replace `ThemeThumbnail` theme if-chain with registry lookup: compute `initial` as before, get `thumbnail` from `getMicrositeTheme(theme)`, and apply `thumbnail.container`, `thumbnail.avatar`, and `thumbnail.initial` or equivalent registry fields. Preserve component name, props, card dimensions, `group-hover` scale behavior, server session redirect flow, Prisma query, and existing usage site. Update `scripts/verify-microsite-themes.mjs` to assert list page imports `getMicrositeTheme` and no longer branches on individual theme IDs in `ThemeThumbnail`. Keep D-02 by making dark/light/gradient thumbnail classes visually equivalent to prior thumbnail behavior while moving source to registry.</action>
  <verify>
    <automated>node scripts/verify-microsite-themes.mjs</automated>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/app/dashboard/microsites/page.tsx` imports `getMicrositeTheme` from `@/lib/microsite-themes`.
    - Source assertion: `ThemeThumbnail` does not maintain separate hardcoded theme branches.
    - Behavior assertion: list thumbnails render for `dark`, `light`, `gradient`, `midnight`, `sunset`, `forest`, `mono`, and invalid theme using registry fallback.
    - CLI assertion: verification script, lint, and TypeScript checks pass.
  </acceptance_criteria>
  <done>Dashboard list thumbnails consume registry, eliminating final theme drift point.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual UAT for theme readability and recognition</name>
  <files>src/lib/microsite-themes.ts, src/app/dashboard/microsites/[id]/microsite-editor.tsx, src/app/dashboard/microsites/new/page.tsx, src/app/dashboard/microsites/page.tsx, src/components/microsite-page-client.tsx</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
    @src/lib/microsite-themes.ts
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
    @src/app/dashboard/microsites/new/page.tsx
    @src/app/dashboard/microsites/page.tsx
    @src/components/microsite-page-client.tsx
  </read_first>
  <what-built>Expanded seven-theme registry, create/edit mini preview pickers, server normalization, public renderer registry styles, dashboard thumbnail registry styles.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and use `http://localhost:4000` per AGENTS.md.
    2. In dashboard create flow, confirm seven cards appear: Dark, Light, Gradient, Midnight, Sunset, Forest, Mono.
    3. Confirm each card has mini public-page preview, theme name, tagline, and selected border/checkmark.
    4. Create or edit one microsite per theme and save.
    5. Reload editor and confirm saved selected theme persists.
    6. Open public microsite on mobile-width and desktop-width viewport; confirm background, text, buttons/cards, footer/share surfaces reflect selected theme and remain readable.
    7. Open dashboard microsite list and confirm thumbnail color cue matches chosen theme.
    8. If possible, seed or temporarily inspect invalid theme value and confirm editor/public/list fall back to dark without visible error.
  </how-to-verify>
  <resume-signal>Type "approved" or describe exact theme/viewport/readability issue.</resume-signal>
  <action>Do not change code during checkpoint. Human verifies D-05 through D-09 preview fidelity, D-14/D-15 palette spread, and D-17 readability after automated checks pass. If user reports issue, fix only affected registry classes or picker markup; do not add custom CSS, payment-gated themes, schema changes, drag-and-drop ordering, or route changes.</action>
  <verify>
    <automated>node scripts/verify-microsite-themes.mjs && npm run lint && npx tsc --noEmit</automated>
    <human-check>Manual UAT steps above approved.</human-check>
  </verify>
  <acceptance_criteria>
    - Human assertion: all seven themes recognizable by name, tagline, preview, and list thumbnail.
    - Human assertion: public mobile and desktop pages meet readability expectations for title, description, link cards, share/footer text.
    - Human assertion: old `dark`, `light`, `gradient` themes do not surprise existing owners visually.
    - CLI assertion: verification script, lint, and TypeScript checks pass before approval.
  </acceptance_criteria>
  <done>Human confirms expanded themes are usable, brandable, readable, and centralized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database→dashboard list | Stored theme string may be unknown when thumbnails render. |
| registry→server component className | Only trusted literal class strings from registry should style thumbnails. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Denial of service | `ThemeThumbnail` | low | mitigate | Use `getMicrositeTheme` fallback for invalid stored values. |
| T-01-06 | Tampering | thumbnail classes | low | mitigate | Use registry literal classes only; no user-controlled class construction. |
| T-01-SC | Tampering | npm installs | high | mitigate | No package install in this phase; if executor adds package install, stop for package legitimacy gate. |
</threat_model>

<verification>
Run `node scripts/verify-microsite-themes.mjs`, `npm run lint`, `npx tsc --noEmit`, then complete blocking human visual UAT.
</verification>

<success_criteria>
Final drift point removed; dashboard list thumbnails, create/edit pickers, server actions, and public renderer all use `src/lib/microsite-themes.ts`; human approves readability and recognition.
</success_criteria>

<output>
Create `.planning/phases/01-microsite-theme-variants/01-02-SUMMARY.md` when done.
</output>
