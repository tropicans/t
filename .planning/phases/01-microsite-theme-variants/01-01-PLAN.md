---
phase: 01-microsite-theme-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-microsite-themes.mjs
  - src/lib/microsite-themes.ts
  - src/app/actions/microsite.ts
  - src/app/dashboard/microsites/[id]/microsite-editor.tsx
  - src/app/dashboard/microsites/new/page.tsx
  - src/components/microsite-page-client.tsx
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05
must_haves:
  truths:
    - "As a microsite owner, I can choose from seven preset themes in create and edit flows per D-01, D-03, THEME-01."
    - "As a microsite owner, I can recognize each theme by mini public-page preview, name, and tagline before saving per D-05, D-06, D-07, THEME-02."
    - "Saved theme persists through existing create/update actions, reload, and public render per THEME-03, THEME-04."
    - "Unknown stored or submitted themes fall back to dark silently per D-10, D-11, D-12, D-13."
    - "Existing dark, light, gradient public visuals remain unchanged per D-02."
  artifacts:
    - scripts/verify-microsite-themes.mjs
    - src/lib/microsite-themes.ts
  key_links:
    - "src/app/actions/microsite.ts imports normalizeMicrositeTheme from registry before Prisma writes."
    - "Editor/create hidden theme value comes from normalized registry ID, not local arrays."
    - "Public renderer imports getMicrositeTheme and uses registry public classes."
---

## Phase Goal

**As a** microsite owner, **I want to** choose from expanded preset themes, **so that** my public microsite better matches my brand.

<objective>
Create first vertical theme slice: test gate, shared registry, create/edit picker, server normalization, and public render fallback for D-01 through D-17.

Purpose: user can select expanded theme presets and see saved selection reflected publicly without schema changes.
Output: registry module, static verification script, updated actions, updated create/edit theme pickers, updated public renderer.
</objective>

<execution_context>
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
@.planning/phases/01-microsite-theme-variants/01-RESEARCH.md
@.planning/phases/01-microsite-theme-variants/01-PATTERNS.md
@src/components/microsite-page-client.tsx
@src/app/actions/microsite.ts
@src/app/dashboard/microsites/[id]/microsite-editor.tsx
@src/app/dashboard/microsites/new/page.tsx
</context>

## Artifacts this phase produces

- New file: `src/lib/microsite-themes.ts`
- New file: `scripts/verify-microsite-themes.mjs`
- New constant: `DEFAULT_MICROSITE_THEME_ID`
- New constant: `MICROSITE_THEMES`
- New type: `MicrositeThemeId`
- New function: `isMicrositeThemeId(value: string): value is MicrositeThemeId`
- New function: `normalizeMicrositeTheme(value: FormDataEntryValue | string | null | undefined): MicrositeThemeId`
- New function: `getMicrositeTheme(value: string | null | undefined)`
- New registry fields: `id`, `label`, `tagline`, `preview`, `public`, `thumbnail`

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Expanded themes save and public render | COVERED | Plan 01 tasks 2-3 |
| REQ | THEME-01 | COVERED | Plan 01 task 3 |
| REQ | THEME-02 | COVERED | Plan 01 task 3 |
| REQ | THEME-03 | COVERED | Plan 01 task 2 and task 3 |
| REQ | THEME-04 | COVERED | Plan 01 task 3 |
| REQ | THEME-05 | COVERED | Plan 01 task 2; Plan 02 task 1 |
| RESEARCH | No package install; literal Tailwind registry | COVERED | Plan 01 task 2 |
| RESEARCH | No Prisma schema change | COVERED | Plan 01 all tasks |
| RESEARCH | Fix theme drift across edit/create/public/list | COVERED | Plan 01 and Plan 02 |
| CONTEXT | D-01..D-17 locked decisions | COVERED | Plan 01 tasks 2-3; Plan 02 task 1 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing registry-consumer verification for expanded theme slice</name>
  <files>scripts/verify-microsite-themes.mjs</files>
  <read_first>
    @.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
    @.planning/phases/01-microsite-theme-variants/01-RESEARCH.md
    @.planning/phases/01-microsite-theme-variants/01-PATTERNS.md
    @src/components/microsite-page-client.tsx
    @src/app/actions/microsite.ts
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
    @src/app/dashboard/microsites/new/page.tsx
  </read_first>
  <behavior>
    - RED: before implementation, script fails because `src/lib/microsite-themes.ts` does not exist and local theme maps still exist.
    - GREEN: script passes when seven stable IDs exist, registry helpers exist, consumers import registry, and local drift points are absent.
  </behavior>
  <action>Create `scripts/verify-microsite-themes.mjs` using Node `fs`/`path` only. Assert `src/lib/microsite-themes.ts` contains `DEFAULT_MICROSITE_THEME_ID`, `MICROSITE_THEMES`, `MicrositeThemeId`, `isMicrositeThemeId`, `normalizeMicrositeTheme`, `getMicrositeTheme`, and exact IDs `dark`, `light`, `gradient`, `midnight`, `sunset`, `forest`, `mono` per D-01 and D-03. Assert editor/create/public/action files import registry helpers. Assert public renderer has no local theme style map and editor/create have no local theme array. Use comment-free negative checks or scoped string checks so verifier text does not self-match. Do not install packages.</action>
  <verify>
    <automated>node scripts/verify-microsite-themes.mjs exits non-zero before Task 2; after Task 3 it exits 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: script names all seven D-01 IDs and fails if any missing.
    - Source assertion: script fails if consumers keep local theme definitions instead of registry imports.
    - CLI assertion: `node scripts/verify-microsite-themes.mjs` fails before implementation, then passes after implementation.
  </acceptance_criteria>
  <done>Static verification guard exists and catches registry/consumer drift for THEME-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create shared theme registry and normalize server writes</name>
  <files>src/lib/microsite-themes.ts, src/app/actions/microsite.ts</files>
  <read_first>
    @src/components/microsite-page-client.tsx
    @src/lib/utils.ts
    @src/app/actions/microsite.ts
    @.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
    @.planning/phases/01-microsite-theme-variants/01-RESEARCH.md
    @.planning/phases/01-microsite-theme-variants/01-PATTERNS.md
  </read_first>
  <behavior>
    - `normalizeMicrositeTheme("sunset")` returns `sunset`; invalid submitted theme values return `dark` per D-10, D-11, D-12, D-13.
    - `updateMicrosite` distinguishes omitted optional fields from submitted invalid values using `formData.has(...)`; omitted `theme`, `description`, `coverImage`, and `avatarImage` preserve current saved values during partial submissions.
    - Registry exposes display metadata reusable by editor previews, public styles, and thumbnails per D-04.
    - Existing `dark`, `light`, `gradient` public classes match current renderer strings per D-02.
  </behavior>
  <action>Create `src/lib/microsite-themes.ts` with typed literal registry. Include seven total themes per D-01, stable string IDs per D-03, display `label` plus `tagline` per D-07, `preview` classes that model mini public pages per D-05 and D-06, `public` classes for renderer, and `thumbnail` classes for dashboard list per D-04. Copy existing `dark`, `light`, and `gradient` public class strings from `src/components/microsite-page-client.tsx` exactly per D-02. Add new professional, brandable palettes for `midnight`, `sunset`, `forest`, `mono` with balanced dark/light/warm/cool/high-contrast spread per D-14 and D-15, changing only colors/surfaces per D-16 and keeping readable contrast per D-17. In `src/app/actions/microsite.ts`, import `normalizeMicrositeTheme`; use it in `createMicrosite` before Prisma writes. In `updateMicrosite`, use `formData.has("theme")` or equivalent to separate omitted theme from submitted theme: when theme field is absent, keep existing `microsite.theme`; when theme field is present but invalid, normalize submitted value to `dark` per D-11. Apply same partial-submission preservation rule to omitted `description`, `coverImage`, `avatarImage`, and other optional fields currently accepted by `updateMicrosite`: omitted means keep current saved value, submitted empty/invalid means apply existing intentional clearing or normalization behavior. Preserve publish-toggle partial updates so toggling `isPublished` cannot wipe saved theme, description, cover image, or avatar image. Preserve `Microsite.theme` as string; do not edit `prisma/schema.prisma` per D-13. Preserve access guards, slug validation, revalidation, and existing mutation names.</action>
  <verify>
    <automated>node scripts/verify-microsite-themes.mjs</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/lib/microsite-themes.ts` exports registry, helpers, seven IDs, display metadata, public classes, preview classes, thumbnail classes.
    - Source assertion: `src/app/actions/microsite.ts` calls `normalizeMicrositeTheme` for create and for update only when submitted theme field is present; absent update theme preserves `microsite.theme`.
    - Source assertion: `updateMicrosite` uses `formData.has(...)` or equivalent for optional partial-update fields so omitted `description`, `coverImage`, `avatarImage`, and theme values preserve existing saved values.
    - Source assertion: no Prisma schema file is modified.
    - Behavior assertion: invalid submitted `FormData.theme` normalizes to `dark` instead of throwing user-facing error.
    - Behavior assertion: publish-toggle or other partial update that omits theme and media/description fields preserves existing saved theme, description, cover image, and avatar image.
    - CLI assertion: `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>Shared registry exists; create/update writes only normalized theme IDs while preserving string schema and access checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire create/edit pickers and public renderer to registry</name>
  <files>src/app/dashboard/microsites/[id]/microsite-editor.tsx, src/app/dashboard/microsites/new/page.tsx, src/components/microsite-page-client.tsx</files>
  <read_first>
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
    @src/app/dashboard/microsites/new/page.tsx
    @src/components/microsite-page-client.tsx
    @src/lib/microsite-themes.ts
    @.planning/phases/01-microsite-theme-variants/01-CONTEXT.md
    @.planning/phases/01-microsite-theme-variants/01-PATTERNS.md
  </read_first>
  <behavior>
    - Create and edit screens render seven theme choices in 3-column desktop grid per D-01 and D-08.
    - Each picker card shows mini public-page preview, label, tagline, border, and checkmark selected state per D-05, D-06, D-07, D-09.
    - Unknown saved theme initializes selected state to `dark` silently per D-12.
    - Public renderer uses registry public classes and falls back to dark for unknown stored theme per D-10.
  </behavior>
  <action>In editor, delete local theme array; import `MICROSITE_THEMES` and `normalizeMicrositeTheme`; initialize `selectedTheme` from normalized `microsite.theme`; keep hidden input named `theme`; render registry cards with `grid grid-cols-3 gap-3`, miniature background, avatar/dot, title strip, two link-card shapes, label, tagline, border selected state, and checkmark. In create page, delete local theme array and use same registry-powered mini preview treatment while preserving `formData.set("theme", selectedTheme)` and redirect after create. In public renderer, delete local theme style object; import `getMicrositeTheme`; set `styles` from `getMicrositeTheme(microsite.theme).public`; keep polling, link hrefs, ShareBar wiring, typography, layout, spacing, and animation unchanged per D-16. Avoid dynamic Tailwind class construction; registry classes must remain literal strings.</action>
  <verify>
    <automated>node scripts/verify-microsite-themes.mjs</automated>
    <automated>npm run lint</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: editor/create map over `MICROSITE_THEMES` and no longer define local theme arrays.
    - Source assertion: public renderer uses `getMicrositeTheme(microsite.theme).public` and no local theme style object.
    - Behavior assertion: user can choose any of seven themes in create/edit UI and submit existing forms.
    - Behavior assertion: existing `dark`, `light`, `gradient` public layouts retain prior class strings.
    - CLI assertion: `node scripts/verify-microsite-themes.mjs`, `npm run lint`, and `npx tsc --noEmit` pass.
  </acceptance_criteria>
  <done>Create/edit/public path consumes same registry; expanded theme choice reaches public render with safe fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard client→server action | Hidden theme field is untrusted user input. |
| database→public client renderer | Stored legacy theme string may be invalid. |
| registry→Tailwind classes | Only trusted literal class strings should reach className. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `createMicrosite`, `updateMicrosite` | medium | mitigate | Normalize submitted theme through `normalizeMicrositeTheme` before Prisma writes. |
| T-01-02 | Elevation of privilege | `updateMicrosite` | high | mitigate | Preserve `getEditableMicrosite` ownership check before updates. |
| T-01-03 | Denial of service | `MicrositePageClient` | low | mitigate | Use `getMicrositeTheme` fallback to default dark for invalid DB values. |
| T-01-04 | Tampering | Tailwind class names | medium | mitigate | Map trusted theme IDs to literal registry classes; never concatenate user-provided class names. |
| T-01-SC | Tampering | npm installs | high | mitigate | No package install in this phase; if executor adds package install, stop for package legitimacy gate. |
</threat_model>

<verification>
Run `node scripts/verify-microsite-themes.mjs`, `npm run lint`, and `npx tsc --noEmit` after Task 3. No Prisma generate needed because schema untouched.
</verification>

<success_criteria>
Seven preset themes exist in one registry; create/edit pickers show mini public previews; server actions normalize theme values; public renderer uses saved theme and falls back to dark for unknown values; THEME-01..THEME-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-microsite-theme-variants/01-01-SUMMARY.md` when done.
</output>
