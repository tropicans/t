---
phase: 06-performance-optimization-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/dashboard/page.tsx
  - src/app/dashboard/analytics/page.tsx
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/app/actions/short.test.ts
  - src/app/actions/short-link-redirect.test.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - TEST-01
must_haves:
  truths:
    - "Prisma client successfully loads schema with indexes on ShortLinkClick and MicrositeClick"
    - "Dashboard overview and analytics queries run concurrently using Promise.all"
    - "Vitest integration suite runs successfully and verifies short link and redirect server actions"
  artifacts:
    - vitest.config.ts
    - src/app/actions/short.test.ts
    - src/app/actions/short-link-redirect.test.ts
  key_links:
    - "prisma/schema.prisma contains indices for ShortLinkClick and MicrositeClick"
    - "src/app/dashboard/page.tsx uses Promise.all"
    - "src/app/dashboard/analytics/page.tsx uses Promise.all"
---

## Phase Goal

**As a** developer, **I want to** optimize dashboard queries and introduce an automated testing suite, **so that** dashboard performance is improved and server actions are regression-tested automatically.

<objective>
Implement performance optimizations and establish a Vitest unit testing suite per decisions D-01 through D-07.

Purpose: add database indexes to query-heavy analytics tables, parallelize dashboard counts to reduce latency, and write tests for core server actions.
Output: schema updates, optimized server pages, testing configuration, and new test files.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-performance-optimization-testing/06-CONTEXT.md
</context>

## Artifacts this phase produces

- New file: `vitest.config.ts`
- New file: `src/app/actions/short.test.ts`
- New file: `src/app/actions/short-link-redirect.test.ts`

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| REQ | PERF-01 | COVERED | Task 1 |
| REQ | PERF-02 | COVERED | Task 2 |
| REQ | TEST-01 | COVERED | Task 3 |
| CONTEXT | D-01, D-02 | COVERED | Task 1 |
| CONTEXT | D-03, D-04 | COVERED | Task 2 |
| CONTEXT | D-05, D-06, D-07 | COVERED | Task 3 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Database Indexes for Analytics Clicks</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    @prisma/schema.prisma
  </read_first>
  <behavior>
    - Add indices on `ShortLinkClick` (`shortLinkId`, `createdAt`) and `MicrositeClick` (`micrositeId`, `linkId`, `createdAt`) in `prisma/schema.prisma`.
    - Run database migrations locally using `npx prisma migrate dev` to generate migration SQL.
  </behavior>
  <action>Add `@@index` annotations to the `ShortLinkClick` and `MicrositeClick` models in `prisma/schema.prisma`. Run `npx prisma migrate dev --name add_analytics_indexes` to generate the migration folder and files, then run `npx prisma generate`.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains the correct indexes.
    - SQL migration file is generated in `prisma/migrations/`.
  </acceptance_criteria>
  <done>Database indexes added and migration generated.</done>
</task>

<task type="auto">
  <name>Task 2: Parallelize Overview and Analytics Dashboard Queries</name>
  <files>src/app/dashboard/page.tsx, src/app/dashboard/analytics/page.tsx</files>
  <read_first>
    @src/app/dashboard/page.tsx
    @src/app/dashboard/analytics/page.tsx
  </read_first>
  <behavior>
    - Reorganize sequential db counts and fetches in `src/app/dashboard/page.tsx` and `src/app/dashboard/analytics/page.tsx` into concurrent `Promise.all` blocks.
  </behavior>
  <action>Refactor the query loading sections in both pages to invoke the Prisma queries within a single array passed to `Promise.all`. Extract individual variables from the returned destructured tuple.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both dashboard files use `Promise.all` for database queries.
    - Code compiles cleanly without errors.
  </acceptance_criteria>
  <done>Dashboard queries parallelized.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Vitest and Write Test Suites</name>
  <files>package.json, vitest.config.ts, src/app/actions/short.test.ts, src/app/actions/short-link-redirect.test.ts</files>
  <read_first>
    @package.json
  </read_first>
  <behavior>
    - Configure Vitest in `package.json` and `vitest.config.ts`.
    - Add tests for `short.ts` and `short-link-redirect.ts`.
    - Assert correct outcomes under mocked authentication states and mock database configurations.
  </behavior>
  <action>Install Vitest dependencies, create `vitest.config.ts`, add test target scripts to `package.json`, and implement unit tests utilizing Vitest's mocking features to simulate database rows, route redirection, and authentication credentials.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` executes successfully and all 12 assertions pass.
  </acceptance_criteria>
  <done>Vitest integration and test suite successfully configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database query boundaries | Indirection of user ID matching prevents scanning other users' analytics data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | Server actions test suites | low | mitigate | Mock NextAuth getServerSession explicitly for both authenticated and anonymous branches to verify access policies. |
</threat_model>

<verification>
Run `npm run test`, `npm run lint` on the modified files, and `npx tsc --noEmit` to verify type and style checks.
</verification>

<success_criteria>
Database indexing, dashboard query parallelization, and Vitest test suite are fully operational and verified.
</success_criteria>

<output>
Create `.planning/phases/06-performance-optimization-testing/06-01-SUMMARY.md` when done.
</output>
