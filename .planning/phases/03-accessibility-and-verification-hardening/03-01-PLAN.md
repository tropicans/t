---
phase: 03-accessibility-and-verification-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 02-drag-and-drop-link-ordering
files_modified:
  - scripts/verify-accessibility.mjs
  - src/app/dashboard/microsites/[id]/microsite-editor.tsx
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - VER-01
  - VER-02
  - VER-03
must_haves:
  truths:
    - "Focus is preserved on chevron buttons after keyboard move operations, shifting to the opposite chevron at list boundaries per D-01, D-02, D-03, UX-01."
    - "Drag handles are hidden from screen readers/keyboard via aria-hidden, while chevrons carry descriptive Indonesian aria-labels per D-04, D-16, UX-01."
    - "Visual drop indicators show exact insertion points during dragover (top/bottom split-calculated) and dragged items are dimmed per D-05, D-06, D-07, UX-02."
    - "A transient green 'Tersimpan ✓' is shown next to the Links title for 1.5s after a successful reorder save per D-08, UX-02."
    - "ARIA live announcements in Indonesian are spoken dynamically on reorder save start and success per D-13, D-14, D-15, UX-01."
    - "Mobile viewports use a two-row list layout (actions stacked on second row with top border) and stack form input fields vertically per D-09, D-10, D-11, UX-03."
    - "Static verification scripts assert these accessibility and responsive design contracts are present in code per VER-01, VER-02, VER-03."
  artifacts:
    - scripts/verify-accessibility.mjs
  key_links:
    - "src/app/dashboard/microsites/[id]/microsite-editor.tsx implements focus management, drag indicator lines, mobile rows, and aria-live announcements."
---

## Phase Goal

**As a** microsite owner, **I want to** reorder my links with keyboard navigation chevrons or screen readers, see clean visual drag/drop boundaries, and view actions clearly formatted on mobile, **so that** the reordering interface is fully accessible, responsive, and robust under all conditions.

<objective>
Implement dynamic focus restoration, boundary chevron shifting, ARIA live announcements in Bahasa Indonesia, relative-positioned drop indicators, transient success labels, and mobile two-row flex layouts.
Ensure typescript checks and lint checks pass cleanly.
</objective>

<execution_context>
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/yudhiar/Downloads/oprek/Dev/url-shortener/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-accessibility-and-verification-hardening/03-CONTEXT.md
@.planning/phases/03-accessibility-and-verification-hardening/03-RESEARCH.md
@src/app/dashboard/microsites/[id]/microsite-editor.tsx
</context>

## Artifacts this phase produces

- New file: `scripts/verify-accessibility.mjs`
- Modified component: `MicrositeEditor` in `src/app/dashboard/microsites/[id]/microsite-editor.tsx`

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| GOAL | Accessible, responsive, and verified themes/reorder | COVERED | Task 2 (a11y), Task 3 (responsiveness), Task 4 (UAT) |
| REQ | UX-01 | COVERED | Task 2 (focus restoration, boundaries, aria labels, live region) |
| REQ | UX-02 | COVERED | Task 3 (drop indicators, transient success, opacity dimming) |
| REQ | UX-03 | COVERED | Task 3 (mobile two-row layout, vertical input stack, max-w-md public container) |
| REQ | VER-01 | COVERED | Task 1 (verification script), Task 2 (npm run lint validation) |
| REQ | VER-02 | COVERED | Task 2, Task 3 (npx tsc --noEmit validation) |
| REQ | VER-03 | COVERED | Task 4 (Manual visual verification checklist) |
| CONTEXT | D-01 through D-16 | COVERED | Task 2 (D-01..D-04, D-13..D-16), Task 3 (D-05..D-07, D-08, D-09..D-12) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add verification script for accessibility and layout checks</name>
  <files>scripts/verify-accessibility.mjs</files>
  <read_first>
    @.planning/phases/03-accessibility-and-verification-hardening/03-CONTEXT.md
    @.planning/phases/03-accessibility-and-verification-hardening/03-RESEARCH.md
  </read_first>
  <behavior>
    - Verification script statically validates that `src/app/dashboard/microsites/[id]/microsite-editor.tsx` contains `aria-live="polite"` or `aria-live` attribute.
    - Script asserts the presence of `aria-hidden="true"` on the drag handles to hide them from assistive technology.
    - Script checks for chevrons `aria-label` interpolation containing their respective titles.
    - Script validates responsive layout patterns such as mobile second row actions toolbar styles (`border-t border-zinc-800/60` or similar).
  </behavior>
  <action>Create a new verification script at `scripts/verify-accessibility.mjs` using node fs module. The script reads `src/app/dashboard/microsites/[id]/microsite-editor.tsx` and asserts that accessibility features (ARIA live region, chevron aria-labels, aria-hidden drag handles, mobile two-row layout classes) are implemented. The script exits 0 if all assertions pass, and 1 if any fails.</action>
  <verify>
    <automated>node scripts/verify-accessibility.mjs</automated>
  </verify>
  <done>Verification script created and exits with failure because implementation is pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement focus management, keyboard flow, and ARIA live announcements</name>
  <files>src/app/dashboard/microsites/[id]/microsite-editor.tsx</files>
  <read_first>
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
    @.planning/phases/03-accessibility-and-verification-hardening/03-CONTEXT.md
    @.planning/phases/03-accessibility-and-verification-hardening/03-RESEARCH.md
  </read_first>
  <behavior>
    - Chevron buttons keep focus on the clicked action card when moved.
    - Focus shifts to the opposite chevron button on the same card if a boundary (top or bottom) makes the clicked button disabled.
    - Drag handles have `aria-hidden="true"` and no `tabIndex` so they are excluded from keyboard navigation.
    - Dedicated visually-hidden screen reader region (`className="sr-only"` and `aria-live="polite"`) updates text on reorder operations in Indonesian (Bahasa Indonesia).
    - Reorder announcements: `"Mengurutkan..."` on start and `"Tautan '[Title]' berhasil dipindahkan ke posisi [Index] dari [Total]"` on success.
    - Custom chevron `aria-label` descriptions include target link title (e.g. `aria-label='Pindahkan "[Title]" ke atas'`).
  </behavior>
  <action>In `src/app/dashboard/microsites/[id]/microsite-editor.tsx`, introduce a `focusTarget` state to hold `{ id: string, direction: 'up' | 'down' }`. In `handleMoveUp` and `handleMoveDown`, compute the target button's next index. If the index reaches a boundary (0 for up, linksState.length - 1 for down), set the `focusTarget` to target the opposite chevron (down for up, up for down). Otherwise, target the same chevron direction. Implement a `useEffect` hook listening to `linksState` and `focusTarget` that queries `document.getElementById(...)` using unique IDs `btn-up-${link.id}` and `btn-down-${link.id}`, triggers `.focus()`, and resets the state. Add `aria-hidden="true"` to drag handles. Add a state `announcement` and render a `div` with class `sr-only` and `aria-live="polite"`. Update the state to `"Mengurutkan..."` on action trigger, and to the dynamic success message when the action completes. Interpolate `link.title` inside chevron `aria-label`s.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src</automated>
  </verify>
  <done>Focus management and ARIA live regions implemented in the campaign editor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement drag & drop drop lines, success states, and responsive layout rows</name>
  <files>src/app/dashboard/microsites/[id]/microsite-editor.tsx</files>
  <read_first>
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
    @.planning/phases/03-accessibility-and-verification-hardening/03-CONTEXT.md
    @.planning/phases/03-accessibility-and-verification-hardening/03-RESEARCH.md
  </read_first>
  <behavior>
    - Dropping a card computes target index by checking if mouse is in the upper 50% (top) or lower 50% (bottom) of the hovered card.
    - Dragover renders a blue line (`bg-blue-500`) exactly above or below the hovered card depending on relative Y position.
    - Saving successfully displays a green `'Tersimpan ✓'` text next to the "Links" title, fading out after 1.5 seconds.
    - Mobile layout stacks link card info and actions into two rows: first row has drag handle and text; second row has the right-aligned actions toolbar separated by a top border.
    - Form fields in add/edit states are stacked vertically on mobile viewports.
  </behavior>
  <action>In `src/app/dashboard/microsites/[id]/microsite-editor.tsx`, track `dragOverIndex` and `dragPosition` (`'top' | 'bottom' | null`) states. In `handleDragOver`, compute `e.clientY - rect.top` and update `dragPosition` depending on whether Y is less than half the height. In `handleDrop` and `handleDragLeave`, reset these states. Restructure `handleDrop` index calculation to insert item before or after target based on `dragPosition`. Render the blue indicator line `div` above or below cards dynamically in the list. Restructure the list card layout to separate content into two rows (`flex flex-col md:flex-row`). Style the second row with `border-t border-zinc-800/60 pt-3 mt-1 md:border-t-0 md:pt-0 md:mt-0 flex justify-end items-center gap-1`. Add `showSaveSuccess` state, setting it to true for 1.5 seconds on successful save, and render `'Tersimpan ✓'` next to "Links" header title.</action>
  <verify>
    <automated>node scripts/verify-accessibility.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint src</automated>
  </verify>
  <done>Responsive two-row layouts and dragover drop lines configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual visual UAT verification of accessibility and responsiveness</name>
  <files>src/app/dashboard/microsites/[id]/microsite-editor.tsx</files>
  <read_first>
    @AGENTS.md
    @.planning/phases/03-accessibility-and-verification-hardening/03-CONTEXT.md
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
  </read_first>
  <what-built>Focus restoration system, boundary focus shifting, visually-hidden ARIA live regions, relative drop line positioning, transient green success checks, mobile two-row layout and stacked edit inputs.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the microsite dashboard editor (`http://localhost:4000/dashboard/microsites/[id]`).
    2. Focus a Move Up chevron button using Tab and press Space/Enter. Check that the link moves, and that the browser focus is correctly restored to the Chevron Up button in its new position.
    3. Click Move Up on the second item to move it to the top. Confirm that focus automatically shifts to the Move Down chevron of that item (since Move Up is now disabled at index 0).
    4. Drag a card over another. Verify that a blue horizontal line appears dynamically on the top or bottom half of the hovered card depending on cursor position.
    5. Perform a drag/drop. Verify that as soon as the save completes, a green "Tersimpan ✓" text appears next to the "Links" title and disappears after 1.5s.
    6. Switch the browser to mobile viewport mode (e.g. 375px width). Verify that each card renders on two rows (handle and title on row 1, action buttons toolbar with a top border on row 2).
    7. Click "Tambah Link" and verify inputs are stacked vertically on mobile viewports.
    8. Check the public page `http://localhost:4000/[username]` in mobile view to confirm it remains centered, readable, and fully functional.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/accessibility issues observed.</resume-signal>
  <action>Visual checks only. Verify accessibility and responsive layout work perfectly under keyboard/mouse controls.</action>
  <verify>
    <automated>node scripts/verify-accessibility.mjs && npx eslint src && npx tsc --noEmit</automated>
    <human-check>Manual accessibility and mobile layout verification approved.</human-check>
  </verify>
  <done>User confirms that accessibility, responsiveness, and visual indicators pass all UAT criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side browser | User triggers interactions via screen reader or keyboard; page must handle focus without crashing. |
| mobile viewports | Touch inputs trigger reordering; touch targets must have sufficient space to prevent adjacent click errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of service | client UI | low | mitigate | Limit focus restoration search to valid DOM IDs. Add safety check if target button element exists before calling `.focus()`. |
| T-03-02 | Information disclosure | screen readers | low | mitigate | ARIA live regions only announce generic non-sensitive reordering status messages in Indonesian, with no PII exposure. |
</threat_model>

<verification>
Run `node scripts/verify-accessibility.mjs`, `npx eslint src`, `npx tsc --noEmit`, and execute manual verification checks.
</verification>

<success_criteria>
Keyboard chevrons maintain focus and shift focus safely at list limits. Screen reader live regions announce changes in Indonesian. Visual drop lines indicate drop locations. Mobile view layouts stack elements cleanly into a two-row design. Static lint and compiler validation passes.
</success_criteria>

<output>
Create `.planning/phases/03-accessibility-and-verification-hardening/03-01-SUMMARY.md` when done.
</output>
