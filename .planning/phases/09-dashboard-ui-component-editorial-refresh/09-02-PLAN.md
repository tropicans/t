---
phase: 09-dashboard-ui-component-editorial-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/app/dashboard/page.tsx
  - src/app/dashboard/links/page.tsx
  - src/app/dashboard/links/short-link-form.tsx
  - src/app/dashboard/links/short-link-list.tsx
  - src/app/dashboard/microsites/page.tsx
  - src/app/dashboard/analytics/page.tsx
  - src/app/dashboard/analytics/analytics-charts.tsx
  - src/app/dashboard/settings/page.tsx
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Dashboard overview page displays editorial serif typography on main headings and uses theme-driven card styles."
    - "Short links page and forms use semantic token styling for inputs, domain prefix badges, and list cards."
    - "Microsites dashboard list page removes legacy blue shadows and borders, using warm terracotta accents and clean status badges."
    - "Analytics page and telemetry charts use warm terracotta palette (#cc785c) instead of legacy blue #3b82f6."
    - "Settings page displays editorial typography and semantic card layout."
    - "All interactive dashboard pages compile cleanly with npx tsc --noEmit."
  artifacts:
    - src/app/dashboard/page.tsx
    - src/app/dashboard/links/page.tsx
    - src/app/dashboard/links/short-link-form.tsx
    - src/app/dashboard/links/short-link-list.tsx
    - src/app/dashboard/microsites/page.tsx
    - src/app/dashboard/analytics/page.tsx
    - src/app/dashboard/analytics/analytics-charts.tsx
    - src/app/dashboard/settings/page.tsx
  key_links:
    - "src/app/dashboard/page.tsx renders h1 with font-serif."
    - "src/app/dashboard/analytics/analytics-charts.tsx uses #cc785c for Area stroke and linearGradient."
---

<objective>
Refactor all dashboard views (`Overview`, `Short Links`, `Microsites`, `Analytics`, and `Settings`) to eliminate legacy hardcoded zinc/blue utility classes, introduce warm terracotta accents, and apply editorial serif typography (`font-serif`) for headers.

Purpose: Deliver a unified, polished Claude editorial user experience across the entire dashboard.
Output: Refactored dashboard page and client components.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-dashboard-ui-component-editorial-refresh/09-RESEARCH.md
@.planning/phases/09-dashboard-ui-component-editorial-refresh/09-01-PLAN.md
@DESIGN.md
@src/app/globals.css
@src/app/dashboard/page.tsx
@src/app/dashboard/links/page.tsx
@src/app/dashboard/links/short-link-form.tsx
@src/app/dashboard/links/short-link-list.tsx
@src/app/dashboard/microsites/page.tsx
@src/app/dashboard/analytics/page.tsx
@src/app/dashboard/analytics/analytics-charts.tsx
@src/app/dashboard/settings/page.tsx
</context>

<tasks>

<task type="auto" id="09-02-01">
  <name>Refresh Overview and Settings Pages</name>
  <read_first>
    - src/app/dashboard/page.tsx
    - src/app/dashboard/settings/page.tsx
    - DESIGN.md
  </read_first>
  <action>
    Refactor `src/app/dashboard/page.tsx`:
    - Update `h1` to `text-3xl font-serif font-bold tracking-tight text-foreground`.
    - Update subtitle to `text-muted-foreground mt-1`.
    - Update metric cards: remove `bg-zinc-900/50 border-zinc-800`, use standard `<Card>` (which already provides `bg-card border-border`).
    - Metric titles: `text-sm font-medium text-muted-foreground`.
    - Metric numbers: `text-2xl font-bold text-foreground`.
    - Links: `text-primary hover:text-terracotta-active`.
    - Quick actions:
      - Buat Microsite: `Button` default (warm terracotta).
      - Short Link: `Button variant="outline"`.

    Refactor `src/app/dashboard/settings/page.tsx`:
    - Update `h1` to `text-3xl font-serif font-bold tracking-tight text-foreground`.
    - Update subtitle to `text-muted-foreground`.
    - Update Account Card: remove `bg-zinc-900/50 border-zinc-800`.
    - Update text labels to `text-muted-foreground` and text values to `text-foreground`.
  </action>
  <acceptance_criteria>
    - `src/app/dashboard/page.tsx` contains `font-serif`
    - `src/app/dashboard/page.tsx` does NOT contain `border-zinc-800`
    - `src/app/dashboard/settings/page.tsx` contains `font-serif`
    - `src/app/dashboard/settings/page.tsx` does NOT contain `border-zinc-800`
  </acceptance_criteria>
  <verify>
    <command>npx tsc --noEmit</command>
  </verify>
</task>

<task type="auto" id="09-02-02">
  <name>Refresh Short Links Page and Components</name>
  <read_first>
    - src/app/dashboard/links/page.tsx
    - src/app/dashboard/links/short-link-form.tsx
    - src/app/dashboard/links/short-link-list.tsx
    - DESIGN.md
  </read_first>
  <action>
    Refactor `src/app/dashboard/links/page.tsx`:
    - Heading `h1`: `text-3xl font-serif font-bold tracking-tight text-foreground`.
    - Subtitle: `text-muted-foreground`.
    - Section title `h2`: `text-xl font-serif font-semibold text-foreground mb-4`.
    - Empty state: `border-border border-dashed bg-card/40 rounded-xl text-muted-foreground`.

    Refactor `src/app/dashboard/links/short-link-form.tsx`:
    - Remove `bg-zinc-900/50 border-zinc-800`, use semantic `Card`.
    - Form labels: `text-foreground font-medium`.
    - Input icons: `text-muted-foreground`.
    - Domain prefix badge: `border-border bg-muted text-muted-foreground`.
    - Custom alias & destination URL inputs: rely on semantic `Input` styling without hardcoded `bg-zinc-950 border-zinc-800 focus-visible:ring-blue-500`.
    - Button: standard terracotta `Button`.

    Refactor `src/app/dashboard/links/short-link-list.tsx`:
    - Link item Card: `bg-card border-border hover:border-primary/40 transition-colors`.
    - Link title and destination: `text-foreground` and `text-muted-foreground`.
    - Action buttons: use semantic `Button variant="ghost"` with `text-muted-foreground hover:text-foreground hover:bg-muted`.
  </action>
  <acceptance_criteria>
    - `src/app/dashboard/links/page.tsx` contains `font-serif`
    - `src/app/dashboard/links/short-link-form.tsx` does NOT contain `focus-visible:ring-blue-500`
    - `src/app/dashboard/links/short-link-form.tsx` does NOT contain `bg-zinc-950`
    - `src/app/dashboard/links/short-link-list.tsx` does NOT contain `border-zinc-800`
  </acceptance_criteria>
  <verify>
    <command>npx tsc --noEmit</command>
  </verify>
</task>

<task type="auto" id="09-02-03">
  <name>Refresh Microsites Page and Analytics Telemetry</name>
  <read_first>
    - src/app/dashboard/microsites/page.tsx
    - src/app/dashboard/analytics/page.tsx
    - src/app/dashboard/analytics/analytics-charts.tsx
    - DESIGN.md
  </read_first>
  <action>
    Refactor `src/app/dashboard/microsites/page.tsx`:
    - Heading `h1`: `text-3xl font-serif font-bold tracking-tight text-foreground`.
    - Subtitle: `text-muted-foreground mt-1 text-sm`.
    - "Buat Microsite" Button: remove `shadow-blue-600/20`, use standard terracotta `Button`.
    - Empty state: `border-border border-dashed bg-card/40 rounded-2xl`.
    - Microsite Cards: `bg-card border-border hover:border-primary/40 rounded-xl p-5 group transition-all` (remove `hover:border-blue-500/40`, `bg-zinc-900/60`).
    - Status badges: Published (`bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border-emerald-500/20`), Draft (`bg-muted text-muted-foreground border-border`).
    - Stats and secondary text: `text-muted-foreground`.
    - Action buttons: `text-muted-foreground hover:text-foreground hover:bg-muted`.

    Refactor `src/app/dashboard/analytics/page.tsx`:
    - Heading `h1`: `text-3xl font-serif font-bold tracking-tight text-foreground`.
    - Subtitle: `text-muted-foreground`.
    - Time range selector: container `bg-muted/60 p-1 rounded-lg border border-border`, items active `bg-card text-foreground shadow-xs font-semibold`, inactive `text-muted-foreground hover:text-foreground`.
    - Stat cards: standard `Card` without `bg-zinc-900/50 border-zinc-800`.
    - Tables / lists: replace `zinc` classes with `border-border`, `text-foreground`, `text-muted-foreground`.

    Refactor `src/app/dashboard/analytics/analytics-charts.tsx`:
    - Update Recharts Area gradient to use `#cc785c` (Claude warm terracotta) with opacities `0.35` and `0`.
    - Update Area `stroke` to `#cc785c`.
    - Update CartesianGrid stroke to `currentColor` with `opacity-10` or theme border `#e6dfd8` / `#2e2b27`.
    - Update XAxis/YAxis stroke to `#8e8b82` (muted soft).
    - Update Tooltip styling: background `var(--card)`, border `var(--border)`, text `var(--foreground)`, itemStyle color `#cc785c`.
  </action>
  <acceptance_criteria>
    - `src/app/dashboard/microsites/page.tsx` contains `font-serif`
    - `src/app/dashboard/microsites/page.tsx` does NOT contain `shadow-blue-600/20`
    - `src/app/dashboard/microsites/page.tsx` does NOT contain `hover:border-blue-500/40`
    - `src/app/dashboard/analytics/analytics-charts.tsx` contains `#cc785c`
    - `src/app/dashboard/analytics/analytics-charts.tsx` does NOT contain `#3b82f6`
  </acceptance_criteria>
  <verify>
    <command>npx tsc --noEmit</command>
  </verify>
</task>

</tasks>

<artifacts_this_phase_produces>
### Artifacts this phase produces
- Editorial serif headings in `src/app/dashboard/page.tsx`
- Refactored `src/app/dashboard/links/page.tsx`, `short-link-form.tsx`, and `short-link-list.tsx`
- Refactored `src/app/dashboard/microsites/page.tsx` removing legacy blue utilities
- Refactored `src/app/dashboard/analytics/page.tsx` and `src/app/dashboard/analytics/analytics-charts.tsx` with terracotta Recharts telemetry
- Refactored `src/app/dashboard/settings/page.tsx`
</artifacts_this_phase_produces>
