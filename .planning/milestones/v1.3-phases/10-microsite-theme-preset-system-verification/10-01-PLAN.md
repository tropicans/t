---
phase: 10-microsite-theme-preset-system-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/microsite-themes.ts
  - src/app/dashboard/microsites/[id]/microsite-editor.tsx
  - src/app/dashboard/microsites/new/page.tsx
  - src/lib/microsite-themes.test.ts
autonomous: true
requirements:
  - THEME-01
  - TEST-01
must_haves:
  truths:
    - "src/lib/microsite-themes.ts registers the 'claude' preset theme with warm terracotta (#cc785c) and tinted cream (#faf9f5) editorial styling."
    - "The public style for 'claude' uses font-serif on title, warm cream canvas (#faf9f5), elevated card surfaces (#efe9de), and terracotta accents."
    - "Microsite editor and creation pages display the 'claude' theme in the selector grid with terracotta selection indicators."
    - "Dedicated unit tests in src/lib/microsite-themes.test.ts verify theme existence, ID guards, and normalization."
    - "All Vitest test suites (18 baseline tests + new theme tests) pass cleanly."
    - "TypeScript compiler (npx tsc --noEmit) validates the entire codebase without errors."
  artifacts:
    - src/lib/microsite-themes.ts
    - src/lib/microsite-themes.test.ts
    - src/app/dashboard/microsites/[id]/microsite-editor.tsx
    - src/app/dashboard/microsites/new/page.tsx
  key_links:
    - "src/lib/microsite-themes.ts exports MICROSITE_THEMES with 'claude' entry consumed by public renderer and dashboard."
    - "src/lib/microsite-themes.test.ts imports isMicrositeThemeId, normalizeMicrositeTheme, and getMicrositeTheme to ensure strict runtime validity."
---

<objective>
Register the `claude` preset theme in `src/lib/microsite-themes.ts`, refine theme selector indicators in the microsite editor and new microsite forms, author comprehensive unit tests in `src/lib/microsite-themes.test.ts`, and execute full system verification with Vitest and `tsc --noEmit`.

Purpose: Complete Milestone v1.3 by providing users a warm terracotta editorial theme for microsites and validating system integrity across all automated test suites.
Output: Registered `claude` theme, updated theme pickers, new Vitest test file, and passing typecheck.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-microsite-theme-preset-system-verification/10-RESEARCH.md
@DESIGN.md
@AGENTS.md
@src/lib/microsite-themes.ts
@src/components/microsite-page-client.tsx
@src/app/dashboard/microsites/[id]/microsite-editor.tsx
@src/app/dashboard/microsites/new/page.tsx
</context>

<tasks>

<task type="auto" id="10-01-01">
  <name>Add 'claude' Preset Theme & Refine Theme Selection UI</name>
  <read_first>
    - src/lib/microsite-themes.ts
    - src/components/microsite-page-client.tsx
    - src/app/dashboard/microsites/[id]/microsite-editor.tsx
    - src/app/dashboard/microsites/new/page.tsx
    - DESIGN.md
  </read_first>
  <action>
    1. In `src/lib/microsite-themes.ts`:
       Add the `claude` preset theme to `MICROSITE_THEMES`:
       ```typescript
       {
         id: "claude",
         label: "Claude",
         tagline: "Editorial hangat terracotta & cream",
         preview: {
           bg: "bg-gradient-to-b from-[#faf9f5] to-[#efe9de]",
           dot: "bg-[#cc785c]",
           card: "bg-[#efe9de] border border-[#e6dfd8]",
         },
         public: {
           page: "bg-[#faf9f5]",
           hero: "from-[#faf9f5]/0 via-[#faf9f5]/60 to-[#faf9f5]",
           title: "text-[#141413] font-serif",
           description: "text-[#3d3d3a]",
           avatar: "border-[#faf9f5] ring-2 ring-[#cc785c]/40",
           card: "bg-[#efe9de] border border-[#e6dfd8] text-[#141413] hover:bg-[#e8e0d2] hover:border-[#cc785c]/40 hover:shadow-lg hover:shadow-[#cc785c]/10",
           cardTitle: "text-[#141413]",
           icon: "text-[#cc785c] group-hover:text-[#a9583e]",
           empty: "text-[#6c6a64]",
           footer: "text-[#8e8b82]",
           footerBrand: "text-[#cc785c]",
           divider: "bg-[#e6dfd8]",
           share: "text-[#6c6a64] hover:text-[#cc785c]",
           shareLabel: "text-[#8e8b82]",
         },
         thumbnail: {
           container: "bg-[#f5f0e8] flex items-center justify-center overflow-hidden group-hover:bg-[#efe9de] transition-all",
           avatar: "bg-[#efe9de] rounded-2xl w-14 h-14 flex items-center justify-center text-[#cc785c] text-2xl font-bold shadow-md group-hover:scale-110 transition-transform border border-[#e6dfd8]",
         },
       }
       ```

    2. In `src/app/dashboard/microsites/[id]/microsite-editor.tsx` and `src/app/dashboard/microsites/new/page.tsx`:
       Refactor the theme selection active state in the grid buttons:
       - Replace `border-blue-500 shadow-lg shadow-blue-500/20` with `border-primary shadow-lg shadow-primary/20`
       - Replace `text-blue-400` with `text-primary`
       - Ensure non-selected cards use consistent styling `border-border hover:border-border/80 bg-card/60`
  </action>
  <acceptance_criteria>
    - `src/lib/microsite-themes.ts` contains `id: "claude"`
    - `src/lib/microsite-themes.ts` includes `tagline: "Editorial hangat terracotta & cream"`
    - `src/lib/microsite-themes.ts` public style contains `font-serif` and `#cc785c`
    - `src/app/dashboard/microsites/[id]/microsite-editor.tsx` contains `border-primary shadow-lg shadow-primary/20`
    - `src/app/dashboard/microsites/new/page.tsx` contains `border-primary shadow-lg shadow-primary/20`
  </acceptance_criteria>
  <verify>
    <command>npx tsc --noEmit</command>
  </verify>
</task>

<task type="auto" id="10-01-02">
  <name>Implement Theme Unit Tests & Run Full System Verification</name>
  <read_first>
    - src/lib/microsite-themes.ts
    - src/app/actions/microsite.test.ts
  </read_first>
  <action>
    1. Create `src/lib/microsite-themes.test.ts`:
       - Test that `isMicrositeThemeId("claude")` returns `true`.
       - Test that `isMicrositeThemeId("unknown-theme")` returns `false`.
       - Test that `normalizeMicrositeTheme("claude")` returns `"claude"`.
       - Test that `normalizeMicrositeTheme("non-existent")` returns `"dark"` (default).
       - Test that `normalizeMicrositeTheme(null)` and `normalizeMicrositeTheme(undefined)` return `"dark"`.
       - Test that `getMicrositeTheme("claude")` returns the theme with label "Claude", tagline containing "terracotta", preview, public styles, and thumbnail.
       - Test that all themes in `MICROSITE_THEMES` have non-empty `id`, `label`, `tagline`, `preview`, `public`, and `thumbnail` configurations.
    2. Run full Vitest suite to verify zero regression across existing 18 tests and new tests.
    3. Run TypeScript type check across the entire project (`npx tsc --noEmit`).
  </action>
  <acceptance_criteria>
    - `src/lib/microsite-themes.test.ts` exists and passes all tests.
    - All 4 test files in Vitest pass (short-link-redirect, microsite, short, and microsite-themes).
    - `npx tsc --noEmit` exits with code 0.
  </acceptance_criteria>
  <verify>
    <command>npx vitest run</command>
  </verify>
</task>

</tasks>

<verification>
1. `npx vitest run` passes all test suites.
2. `npx tsc --noEmit` compiles cleanly with zero errors.
3. `npm run build` succeeds.
</verification>
