---
phase: 09-dashboard-ui-component-editorial-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/app/dashboard/layout.tsx
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "Base UI button component supports Claude primary warm terracotta with terracotta-active hover state and ring tokens."
    - "Base UI card, input, and badge components use theme CSS variables instead of hardcoded zinc or neutral utilities."
    - "Dashboard sidebar and layout shell use semantic sidebar tokens (bg-sidebar, border-sidebar-border, text-sidebar-foreground)."
    - "Active navigation link highlighted with subtle warm terracotta tint (bg-primary/15 text-primary)."
    - "Sign out button and user info area use theme-aware classes without hardcoded zinc colors."
  artifacts:
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/input.tsx
    - src/app/dashboard/layout.tsx
  key_links:
    - "src/components/ui/button.tsx binds default variant to bg-primary hover:bg-terracotta-active."
    - "src/app/dashboard/layout.tsx imports and uses updated Button and semantic sidebar classes."
---

<objective>
Update core interactive UI primitives (Button, Card, Badge, Input) and refactor the Dashboard Layout shell (`src/app/dashboard/layout.tsx`) to adopt Claude warm editorial aesthetics, removing legacy hardcoded zinc utilities and ensuring consistent design system token utilization.

Purpose: Establish the foundational components and dashboard shell upon which all dashboard pages render.
Output: Refactored UI primitives in `src/components/ui/` and refreshed `src/app/dashboard/layout.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-dashboard-ui-component-editorial-refresh/09-RESEARCH.md
@DESIGN.md
@AGENTS.md
@src/app/globals.css
@src/components/ui/button.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/components/ui/input.tsx
@src/app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto" id="09-01-01">
  <name>Refactor UI Primitives (Button, Card, Badge, Input)</name>
  <read_first>
    - src/components/ui/button.tsx
    - src/components/ui/card.tsx
    - src/components/ui/badge.tsx
    - src/components/ui/input.tsx
    - src/app/globals.css
    - DESIGN.md
  </read_first>
  <action>
    Refactor `button.tsx`:
    - In `buttonVariants`, update `default` variant to `bg-primary text-primary-foreground hover:bg-terracotta-active shadow-xs`.
    - Update `outline` variant to `border border-border bg-background shadow-xs hover:bg-muted hover:text-foreground`.
    - Ensure `focus-visible:ring-ring` and active/hover states align with Claude design guidelines.

    Refactor `card.tsx`:
    - Ensure base `Card` uses `bg-card text-card-foreground border border-border rounded-xl shadow-xs`.
    - Ensure `CardDescription` uses `text-muted-foreground`.

    Refactor `badge.tsx`:
    - Ensure `badgeVariants` `default` uses `bg-primary text-primary-foreground`.
    - Ensure `secondary` uses `bg-secondary text-secondary-foreground`.
    - Ensure `outline` uses `border-border text-foreground`.

    Refactor `input.tsx`:
    - Ensure `Input` uses `border-border bg-background/50 text-foreground placeholder:text-muted-foreground focus-visible:border-ring focus-visible:ring-ring/50`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/button.tsx` contains `hover:bg-terracotta-active`
    - `src/components/ui/card.tsx` contains `border-border` and `rounded-xl`
    - `src/components/ui/badge.tsx` contains `border-border`
    - `src/components/ui/input.tsx` contains `border-border` and `focus-visible:ring-ring`
  </acceptance_criteria>
  <verify>
    <command>npx tsc --noEmit</command>
  </verify>
</task>

<task type="auto" id="09-01-02">
  <name>Refresh Dashboard Layout & Navigation Shell</name>
  <read_first>
    - src/app/dashboard/layout.tsx
    - src/components/ui/button.tsx
    - src/app/globals.css
    - DESIGN.md
  </read_first>
  <action>
    Refactor `src/app/dashboard/layout.tsx`:
    - Replace hardcoded `bg-zinc-800`, `border-zinc-800`, `text-white`, `text-zinc-400`, `text-zinc-500` with semantic tokens:
      - Sidebar container: `w-64 border-r border-sidebar-border bg-sidebar text-sidebar-foreground flex flex-col hidden md:flex`.
      - Logo header: `h-16 flex items-center px-6 border-b border-sidebar-border`.
      - Logo text & icon: `font-serif font-bold text-foreground text-lg tracking-tight`, icon wrapper `w-8 h-8 bg-primary text-primary-foreground rounded-lg`.
      - Nav items: active `bg-primary/15 text-primary font-medium`, inactive `text-muted-foreground hover:text-foreground hover:bg-sidebar-accent`.
      - User info section: `border-t border-sidebar-border`, avatar fallback `bg-sidebar-accent text-sidebar-foreground`, user name `text-foreground`, email `text-muted-foreground`.
      - Sign Out button: `variant="ghost" className="w-full justify-start text-muted-foreground hover:text-foreground hover:bg-sidebar-accent"`.
      - Mobile header: `h-16 border-b border-sidebar-border bg-sidebar flex items-center px-4 shrink-0`.
      - Main container: `min-h-screen bg-background text-foreground flex`.
  </action>
  <acceptance_criteria>
    - `src/app/dashboard/layout.tsx` contains `border-sidebar-border`
    - `src/app/dashboard/layout.tsx` contains `bg-sidebar`
    - `src/app/dashboard/layout.tsx` does NOT contain `border-zinc-800`
    - `src/app/dashboard/layout.tsx` does NOT contain `bg-zinc-800`
    - `src/app/dashboard/layout.tsx` contains `hover:bg-sidebar-accent`
  </acceptance_criteria>
  <verify>
    <command>npx tsc --noEmit</command>
  </verify>
</task>

</tasks>

<artifacts_this_phase_produces>
### Artifacts this phase produces
- Updated classes and variant props in `src/components/ui/button.tsx` (`hover:bg-terracotta-active`)
- Updated semantic classes in `src/components/ui/card.tsx`
- Updated semantic classes in `src/components/ui/badge.tsx`
- Updated semantic classes in `src/components/ui/input.tsx`
- Semantic styling and editorial branding in `src/app/dashboard/layout.tsx`
</artifacts_this_phase_produces>
