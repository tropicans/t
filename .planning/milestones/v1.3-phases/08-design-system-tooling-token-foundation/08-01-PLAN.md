---
phase: 08-design-system-tooling-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - AGENTS.md
  - DESIGN.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOKEN-01
  - TOKEN-02
must_haves:
  truths:
    - "Tailwind CSS v4 and globals.css define Claude design system tokens in both dark and light modes."
    - "Primary color token is set to Claude warm terracotta #cc785c with ring #cc785c."
    - "Dark background is set to Claude dark surface #181715 and elevated cards to #252320."
    - "Light canvas is set to Claude tinted cream #faf9f5 with ink foreground #141413."
    - "Typography rules include an editorial serif variable (--font-serif) for display headlines."
    - "Root DESIGN.md is actively maintained and referenced in AGENTS.md."
  artifacts:
    - src/app/globals.css
    - src/app/layout.tsx
    - AGENTS.md
    - DESIGN.md
  key_links:
    - "src/app/globals.css maps CSS variables to Tailwind inline @theme including terracotta and serif tokens."
    - "src/app/layout.tsx injects --font-serif alongside --font-inter into <body>."
---

<objective>
Configure the design system tooling and foundation for the Claude warm terracotta editorial design system. This establishes the active `DESIGN.md` contract, documents agent instructions in `AGENTS.md`, implements Claude color tokens in `src/app/globals.css`, and wires editorial typography in `src/app/layout.tsx`.

Purpose: Provide a unified, robust design token layer that future UI and dashboard components will build upon.
Output: Configured `globals.css`, updated `layout.tsx` font variables, and verified design system documentation.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@DESIGN.md
@AGENTS.md
@src/app/globals.css
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and Document Tooling and DESIGN.md in AGENTS.md</name>
  <files>DESIGN.md, AGENTS.md</files>
  <read_first>DESIGN.md, AGENTS.md</read_first>
  <action>
    Confirm that DESIGN.md is in the project root with the complete Claude design analysis specification (colors, typography, geometry, component styling).
    Ensure AGENTS.md contains explicit design system rules directing all coding agents to respect DESIGN.md, including color tokens (primary terracotta #cc785c, canvas #faf9f5, dark surface #181715) and npx getdesign CLI instructions.
  </action>
  <verify>
    node -e "const fs = require('fs'); const d = fs.readFileSync('DESIGN.md', 'utf8'); const a = fs.readFileSync('AGENTS.md', 'utf8'); console.log(d.includes('#cc785c') && a.includes('DESIGN.md') ? 'OK' : 'MISSING');"
  </verify>
  <acceptance_criteria>
    - DESIGN.md exists and contains "name: Claude-design-analysis"
    - DESIGN.md contains primary color "#cc785c"
    - AGENTS.md contains "## Design System" and explicitly cites "DESIGN.md"
  </acceptance_criteria>
  <done>DESIGN.md and AGENTS.md are aligned as the authoritative design contract.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Claude Palette & Surface Tokens in globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css, DESIGN.md</read_first>
  <action>
    In src/app/globals.css:
    1. Update the `@theme inline` section to expose:
       - `--color-terracotta: #cc785c;`
       - `--color-terracotta-active: #a9583e;`
       - `--color-cream-canvas: #faf9f5;`
       - `--color-dark-surface: #181715;`
       - `--font-serif: var(--font-serif);`
    2. Update `.dark` variables to match Claude's dark surfaces:
       - `--background: #181715;`
       - `--foreground: #faf9f5;`
       - `--card: #252320;`
       - `--card-foreground: #faf9f5;`
       - `--popover: #252320;`
       - `--popover-foreground: #faf9f5;`
       - `--primary: #cc785c;`
       - `--primary-foreground: #ffffff;`
       - `--secondary: #1f1e1b;`
       - `--secondary-foreground: #faf9f5;`
       - `--muted: #1f1e1b;`
       - `--muted-foreground: #a09d96;`
       - `--accent: #cc785c;`
       - `--accent-foreground: #ffffff;`
       - `--border: #2e2b27;`
       - `--input: #2e2b27;`
       - `--ring: #cc785c;`
       - `--sidebar: #181715;`
       - `--sidebar-foreground: #faf9f5;`
       - `--sidebar-primary: #cc785c;`
       - `--sidebar-border: #2e2b27;`
       - `--sidebar-accent: #252320;`
    3. Update `:root` (light mode) variables to match Claude's cream editorial styling:
       - `--background: #faf9f5;`
       - `--foreground: #141413;`
       - `--card: #efe9de;`
       - `--card-foreground: #141413;`
       - `--popover: #f5f0e8;`
       - `--popover-foreground: #141413;`
       - `--primary: #cc785c;`
       - `--primary-foreground: #ffffff;`
       - `--secondary: #f5f0e8;`
       - `--secondary-foreground: #141413;`
       - `--muted: #e8e0d2;`
       - `--muted-foreground: #6c6a64;`
       - `--accent: #cc785c;`
       - `--accent-foreground: #ffffff;`
       - `--border: #e6dfd8;`
       - `--input: #e6dfd8;`
       - `--ring: #cc785c;`
  </action>
  <verify>
    node -e "const fs = require('fs'); const c = fs.readFileSync('src/app/globals.css', 'utf8'); console.log(c.includes('#cc785c') && c.includes('#181715') && c.includes('#faf9f5') ? 'OK' : 'MISSING_TOKENS');"
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains "--primary: #cc785c;" in both :root and .dark
    - src/app/globals.css contains "--background: #181715;" in .dark
    - src/app/globals.css contains "--background: #faf9f5;" in :root
    - src/app/globals.css contains "--font-serif: var(--font-serif);" in @theme inline
  </acceptance_criteria>
  <done>Tailwind CSS v4 variables and custom properties fully reflect Claude's palette.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Editorial Serif Font Variable in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>src/app/layout.tsx, src/app/globals.css</read_first>
  <action>
    In src/app/layout.tsx:
    1. Import `Newsreader` (or `Source_Serif_4`) from `next/font/google` to represent Claude's editorial headlines.
    2. Configure `const newsreaderSerif = Newsreader({ variable: "--font-serif", subsets: ["latin"], style: ["normal", "italic"] });`
    3. Add `newsreaderSerif.variable` to the `<body>` className string so `--font-serif` is available globally in the DOM.
  </action>
  <verify>
    node -e "const fs = require('fs'); const l = fs.readFileSync('src/app/layout.tsx', 'utf8'); console.log(l.includes('--font-serif') && l.includes('next/font/google') ? 'OK' : 'MISSING');"
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx imports a serif font from next/font/google
    - src/app/layout.tsx defines variable "--font-serif"
    - <body> className contains the serif font variable
  </acceptance_criteria>
  <done>Editorial serif typography variable is accessible for headlines across the application.</done>
</task>

<task type="auto">
  <name>Task 4: Automated Verification and TypeScript Check</name>
  <files>vitest.config.ts, src/app/actions/*.test.ts</files>
  <read_first>vitest.config.ts</read_first>
  <action>
    Run `npm run test` with Vitest and `npx tsc --noEmit` to ensure that adding design tokens and font variables does not introduce type errors or break any server action unit tests.
  </action>
  <verify>
    npm run test && npx tsc --noEmit
  </verify>
  <acceptance_criteria>
    - vitest runs and passes all 18 existing unit tests
    - tsc --noEmit exits with code 0 and zero TypeScript diagnostic errors
  </acceptance_criteria>
  <done>Phase 8 foundation passes all automated regression tests cleanly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test` exits 0 with 18 passed tests
- [ ] `npx tsc --noEmit` passes cleanly
- [ ] `src/app/globals.css` defines `--color-terracotta: #cc785c` and proper `.dark` / `:root` color tokens
- [ ] `src/app/layout.tsx` injects `--font-serif`
- [ ] `DESIGN.md` and `AGENTS.md` are committed and synchronized
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Claude design system tokens are active and ready for Phase 9 UI refresh
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-tooling-token-foundation/08-01-SUMMARY.md`
</output>
