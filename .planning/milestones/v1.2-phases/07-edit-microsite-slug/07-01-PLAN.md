---
phase: 07-edit-microsite-slug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/microsite.ts
  - src/app/dashboard/microsites/[id]/microsite-editor.tsx
  - src/app/actions/microsite.test.ts
autonomous: true
requirements:
  - SLUG-01
  - SLUG-02
  - SLUG-03
  - SLUG-04
  - SLUG-05
must_haves:
  truths:
    - "User can edit their microsite slug in the editor UI"
    - "Editing the slug validates against collision with reserved routes and short links"
    - "updateMicrosite server action validates and updates the slug in the database"
    - "Selective path revalidation triggers for old and new slugs"
    - "Vitest unit tests verify slug updates, collisions, and validation outcomes"
  artifacts:
    - src/app/actions/microsite.test.ts
  key_links:
    - "src/app/actions/microsite.ts handles slug updates and collision checks"
    - "src/app/dashboard/microsites/[id]/microsite-editor.tsx contains slug input field"
---

## Phase Goal

**As a** microsite owner, **I want to** update my microsite URL slug and have it verified against collision automatically, **so that** I can customize my public URL path cleanly and securely.

<objective>
Implement slug editing inline in the dashboard editor form with prefix domain decoration and client-side sanitization. Update the updateMicrosite server action with validators and cache revalidations. Implement Vitest unit tests verifying all logic and edge cases.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-edit-microsite-slug/07-CONTEXT.md
</context>

## Artifacts this phase produces

- New file: `src/app/actions/microsite.test.ts`

## Multi-Source Coverage Audit

| Source | Item | Status | Plan coverage |
|--------|------|--------|---------------|
| REQ | SLUG-01 | COVERED | Task 2 |
| REQ | SLUG-02 | COVERED | Task 1 |
| REQ | SLUG-03 | COVERED | Task 1 |
| REQ | SLUG-04 | COVERED | Task 1 |
| REQ | SLUG-05 | COVERED | Task 3 |
| CONTEXT | D-01, D-02, D-03 | COVERED | Task 2 |
| CONTEXT | D-04, D-05, D-06, D-07 | COVERED | Task 1 |

<tasks>

<task type="auto">
  <name>Task 1: Implement Server Action Updates</name>
  <files>src/app/actions/microsite.ts</files>
  <read_first>
    @src/app/actions/microsite.ts
  </read_first>
  <behavior>
    - Add slug verification and validation with validateSlugCollision.
    - Perform selective path revalidation for old and new slugs.
  </behavior>
  <action>Modify updateMicrosite to retrieve the old slug, run validateSlugCollision on the new slug, write updates atomically to the database, and revalidate old and new paths.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - updateMicrosite correctly processes slug.
    - Path revalidation logic matches the specification.
  </acceptance_criteria>
  <done>Server action successfully updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add Slug Input Field in editor UI</name>
  <files>src/app/dashboard/microsites/[id]/microsite-editor.tsx</files>
  <read_first>
    @src/app/dashboard/microsites/[id]/microsite-editor.tsx
  </read_first>
  <behavior>
    - Add input field for slug.
    - Implement client-side lowercase and hyphen character sanitization.
    - Format input with domain prefix segment.
  </behavior>
  <action>Add local state for slug value, add prefix layout segment using window.location.host, hook onChange/onBlur sanitization, and place the input inline in the editor form card.</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Slug input appears inside editor UI form.
    - Input sanitization logic cleans invalid characters.
  </acceptance_criteria>
  <done>UI input field integrated and configured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Vitest Unit Tests</name>
  <files>src/app/actions/microsite.test.ts</files>
  <behavior>
    - Write unit tests targeting updateMicrosite logic.
    - Assert success, collision failure, path revalidations, and database update atomicity.
  </behavior>
  <action>Create a new test file src/app/actions/microsite.test.ts containing Mock Prisma database calls, Mock NextAuth settings, and Mock Next.js Cache functions. Run tests using Vitest.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - npm run test runs with 6 new tests and all pass.
  </acceptance_criteria>
  <done>Unit tests created and executing successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-level authorization boundary | Ensure users can only edit their own microsite slugs via getEditableMicrosite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | updateMicrosite | medium | mitigate | Check validation on collision to prevent routing takeover of existing short links or reserved routes. |
</threat_model>

<verification>
Run npm run test to verify unit tests.
</verification>

<success_criteria>
Slug updating successfully integrated and verified with 100% test coverage of newly added paths.
</success_criteria>

<output>
Create .planning/phases/07-edit-microsite-slug/07-01-SUMMARY.md when done.
</output>
